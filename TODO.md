# TODO

## Высокий приоритет

- [ ] Определить политику обработки будущих дат:
  - запрещать их;
  - выводить предупреждение;
  - либо хранить плановые тренировки отдельно от фактических.
- [ ] Исправить переход между месяцами:
  - синхронизировать заголовок периода с `running-config`;
  - автоматически обновлять ссылку на PNG или использовать стабильное имя файла.
- [ ] Сделать обновление `running-data.org` и PNG согласованным: заменять итоговые файлы только после успешной генерации обоих артефактов.

## Тестирование

- [ ] Добавить ERT-тесты для вычислительных функций.
- [ ] Покрыть тестами:
  - пустой журнал;
  - несколько тренировок в одну дату;
  - будущие даты;
  - прошлый, текущий и будущий месяцы;
  - високосный февраль;
  - невалидные даты и числа;
  - расчёт накопленного результата;
  - расчёт `y-max`.
- [ ] Добавить автоматический запуск тестов в batch-режиме Emacs.

## Документация и переносимость

- [ ] Добавить `README.org` с описанием формата данных, зависимостей и команды `M-x running-update-chart`.
- [ ] Зафиксировать минимальную поддерживаемую версию Emacs.
- [ ] Документировать установку и требования к gnuplot.
- [ ] Сделать шрифт и терминал gnuplot переносимыми между macOS и Linux.
- [ ] Объяснить семантику идеального плана, фактического результата и значений `NaN`.

## Качество данных

- [ ] Предупреждать о строках `running-log`, которые находятся вне месяца из `running-config`.
- [ ] Явно валидировать названия и количество колонок Org-таблиц.
- [ ] Решить, должен ли генерируемый `running-data.org` храниться в Git.
- [ ] Если `running-data.org` остаётся в Git, добавить проверку его актуальности.
- [ ] Рассмотреть поддержку запятой как десятичного разделителя для русской локали.

## Emacs Lisp

- [ ] Добавить `defgroup` и `defcustom` для пользовательских настроек: каталога, gnuplot, шрифта и локали.
- [ ] Убрать зависимость путей от константы, вычисляемой только во время загрузки файла.
- [ ] Решить, нужна ли автоматическая загрузка кода через Local Variables; при необходимости заменить её на документированный `require` или явную загрузку.
- [ ] Не оставлять скрытый буфер `running.org`, если команда открыла его только для генерации графика.

## Визуализация и структура проекта

- [ ] Передавать в gnuplot отдельный признак воскресенья вместо поиска строки `Вс` в подписи.
- [ ] Устранить дублирование расчёта идеальной линии между Emacs Lisp и gnuplot либо документировать его назначение.
- [ ] Продумать структуру хранения и архивирования нескольких месяцев.
- [ ] Убрать executable-бит с `running.org`.
